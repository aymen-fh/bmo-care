<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <style>
    <% if (tajawalFontBase64 && tajawalFontBoldBase64) { %>
    @font-face {
      font-family: 'Tajawal';
      src: url(data:font/ttf;base64,<%= tajawalFontBase64 %>) format('truetype');
      font-weight: 400;
    }
    @font-face {
      font-family: 'Tajawal';
      src: url(data:font/ttf;base64,<%= tajawalFontBoldBase64 %>) format('truetype');
      font-weight: 700;
    }
    <% } %>

    :root {
      --primary: #2563eb;
      --secondary: #ec4899;
      --secondary-dark: #db2777;
      --bg: #f8fafc;
      --card: #ffffff;
      --text: #0f172a;
      --muted: #64748b;
      --success: #22c55e;
      --warning-bg: #fff7ed;
      --warning-border: #fed7aa;
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: <%= (tajawalFontBase64 && tajawalFontBoldBase64) ? "'Tajawal', sans-serif" : "system-ui, Arial, sans-serif" %>;
      background: var(--bg);
      color: var(--text);
    }

    .page {
      padding: 28px;
    }

    /* Header */
    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 14px;
    }

    .brand {
      font-size: 30px;
      font-weight: 900;
      color: var(--primary);
    }

    .subtitle {
      font-size: 13px;
      color: var(--muted);
    }

    .divider {
      height: 3px;
      background: linear-gradient(to left, var(--primary), var(--secondary));
      border-radius: 2px;
      margin-bottom: 18px;
    }

    /* Layout */
    .grid {
      display: grid;
      grid-template-columns: 2.2fr 1fr;
      gap: 18px;
    }

    /* Report Card */
    .report {
      background: linear-gradient(135deg, var(--secondary), var(--secondary-dark));
      color: #fff;
      border-radius: 18px;
      padding: 18px 20px;
      box-shadow: 0 12px 25px rgba(0,0,0,0.12);
    }

    .report h2 {
      margin: 0;
      font-size: 20px;
      font-weight: 900;
    }

    .report-meta {
      margin-top: 10px;
      font-size: 13px;
      line-height: 1.8;
      opacity: 0.95;
    }

    /* Child Card */
    .card {
      background: var(--card);
      border-radius: 18px;
      margin-top: 14px;
      box-shadow: 0 10px 20px rgba(0,0,0,0.08);
      overflow: hidden;
    }

    .card-header {
      background: #fdf2f8;
      padding: 14px 18px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .badge {
      background: var(--secondary);
      color: #fff;
      padding: 6px 14px;
      border-radius: 999px;
      font-size: 12px;
      font-weight: 900;
    }

    .child {
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .avatar {
      width: 38px;
      height: 38px;
      border-radius: 50%;
      background: var(--secondary-dark);
      display: flex;
      align-items: center;
      justify-content: center;
      color: #fff;
      font-weight: 900;
      font-size: 16px;
    }

    .child-name {
      font-weight: 900;
      font-size: 15px;
    }

    .child-age {
      font-size: 12px;
      color: var(--muted);
      margin-top: 2px;
    }

    /* Table */
    table {
      width: 100%;
      border-collapse: collapse;
      font-size: 13px;
    }

    th {
      background: #f1f5f9;
      padding: 12px;
      color: var(--muted);
      font-weight: 900;
    }

    td {
      padding: 14px;
      text-align: center;
      font-weight: 800;
      border-top: 1px solid #e5e7eb;
    }

    /* Progress */
    .progress {
      padding: 14px 18px 18px;
    }

    .progress-info {
      display: flex;
      justify-content: space-between;
      font-size: 12px;
      color: var(--muted);
      margin-bottom: 6px;
      font-weight: 800;
    }

    .bar {
      height: 8px;
      background: #e5e7eb;
      border-radius: 999px;
      overflow: hidden;
    }

    .bar div {
      width: <%= successRate %>%;
      height: 100%;
      background: linear-gradient(to left, #22c55e, #16a34a);
    }

    /* Recommendations */
    .recs {
      background: var(--warning-bg);
      border: 1px solid var(--warning-border);
      border-radius: 18px;
      padding: 16px 18px;
      box-shadow: 0 8px 18px rgba(0,0,0,0.06);
    }

    .recs h3 {
      margin: 0 0 10px;
      font-size: 15px;
      font-weight: 900;
      color: #b45309;
    }

    .recs ul {
      padding-right: 18px;
      margin: 0;
      font-size: 13px;
      line-height: 1.9;
      font-weight: 800;
      color: #7c2d12;
    }

    @page {
      size: A4 landscape;
      margin: 16mm;
    }
  </style>
</head>

<body>
<div class="page">

  <div class="header">
    <div class="subtitle">تطبيق علاج النطق للأطفال</div>
    <div class="brand">نطق</div>
  </div>

  <div class="divider"></div>

  <div class="grid">
    <div>
      <div class="report">
        <h2>تقرير تفصيلي</h2>
        <div class="report-meta">
          <div>اسم الطفل: <%= childName %></div>
          <div>تاريخ التقرير: <%= reportDate %></div>
        </div>
      </div>

      <div class="card">
        <div class="card-header">
          <div class="badge"><%= successRate %>% نسبة النجاح</div>
          <div class="child">
            <div>
              <div class="child-name"><%= childName %></div>
              <div class="child-age"><%= childAgeText %></div>
            </div>
            <div class="avatar"><%= childInitial %></div>
          </div>
        </div>

        <table>
          <thead>
            <tr>
              <th>متوسط الدرجة</th>
              <th>نسبة النجاح</th>
              <th>وقت اللعب</th>
              <th>عدد الجلسات</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td><%= averageScore %></td>
              <td><%= successRate %>%</td>
              <td><%= playTimeText %></td>
              <td><%= totalSessions %></td>
            </tr>
          </tbody>
        </table>

        <div class="progress">
          <div class="progress-info">
            <span>التقدم في النطق</span>
            <span><%= successRate %>%</span>
          </div>
          <div class="bar"><div></div></div>
        </div>
      </div>
    </div>

    <div class="recs">
      <h3>التوصيات</h3>
      <ul>
        <li>تشجيع الطفل على الاستمرارية اليومية.</li>
        <li>زيادة عدد الجلسات الأسبوعية.</li>
        <li>مراقبة التقدم بشكل دوري.</li>
      </ul>
    </div>
  </div>

</div>
</body>
</html>
